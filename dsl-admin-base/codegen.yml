overwrite: true
schema: ${API_ENDPOINT}
documents: '**/*.graphql'
generates:
  graphql/generated/graphql.tsx:
    config:
      withHooks: true
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-react-apollo'
