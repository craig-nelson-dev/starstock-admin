query GetBrandOwnerTopProducts($i: BrandOwnerOverviewInput!) {
  getBrandOwnerTopProducts(input: $i) {
    id
    status {
      displayName
    }
    name
    shortDescription
    longDescription
    code
    totalSales
    thumbnailPath
  }
}
