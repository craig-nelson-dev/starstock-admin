query GetProductPostcodeRestriction($id: Int!) {
  getProductPostcodeRestrictions(productId: $id) {
    postcodes {
      id
      postcode {
        id
        postcode
        defaultAvailable
      }
      deliveryAvailable
      brandLocked
    }
  }
}
